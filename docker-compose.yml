services:
  laneflow-studio:
    build:
      context: .
      target: production
      args:
        API_BASE_URL: ${API_BASE_URL}
    container_name: laneflow-studio
    ports:
      - "${PORT:-4200}:80"
    restart: unless-stopped