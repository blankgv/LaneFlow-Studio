services:
  laneflow-studio:
    build:
      context: .
      target: production
    container_name: laneflow-studio
    ports:
      - "${PORT:-4200}:8080"
    environment:
      - API_BASE_URL=${API_BASE_URL}
      - WS_BASE_URL=${WS_BASE_URL}
    restart: unless-stopped
